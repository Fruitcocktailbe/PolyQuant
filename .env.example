# PolyQuant 2.0 Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# API KEYS
# =============================================================================

# Google Gemini API Key (used for ALL AI operations: Discovery, Logic Architect, Validator)
# Get yours at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# BLOCKCHAIN / DATA PROVIDERS
# =============================================================================

# Alchemy API Key (for Polygon blockchain data)
# Get yours at: https://www.alchemy.com/
ALCHEMY_API_KEY=your-alchemy-key-here

# Polygon Private Key (for EIP-712 signing and trade execution)
# WARNING: This is a sensitive secret. Never commit this to version control.
# Format: 0x followed by 64 hex characters
POLYGON_PRIVATE_KEY=

# Polymarket API endpoints
POLYMARKET_CLOB_URL=https://clob.polymarket.com
POLYMARKET_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws/market

# =============================================================================
# REDIS CACHE
# =============================================================================

REDIS_URL=redis://localhost:6379/0

# =============================================================================
# TRADING PARAMETERS
# =============================================================================

# Target extraction efficiency (0.0 to 1.0)
# How much of available arbitrage to capture (0.9 = 90%)
EXTRACTION_ALPHA=0.9

# Maximum drawdown before kill switch triggers (0.0 to 1.0)
MAX_DRAWDOWN=0.15

# Maximum position size as fraction of order book depth
ORDERBOOK_DEPTH_CAP=0.5

# Target latency in milliseconds
LATENCY_TARGET_MS=30

# VWAP slippage limit as fraction (0.05 = 5% max drift before aborting)
VWAP_SLIPPAGE_LIMIT=0.05

# Polymarket taker fee as fraction (0.02 = 2% per leg)
POLYMARKET_TAKER_FEE_PCT=0.02

# Estimated Polygon gas cost per transaction in USD
POLYGON_GAS_PER_TX=0.30

# =============================================================================
# HTTP & EXECUTION TIMEOUTS
# =============================================================================

# CLOB API timeout in seconds (order books, trading)
CLOB_TIMEOUT_SECONDS=5.0

# Gamma API timeout in seconds (market discovery)
GAMMA_TIMEOUT_SECONDS=10.0

# Max retries for GET requests with exponential backoff
HTTP_MAX_RETRIES=3

# Max time in ms to poll for order confirmation before cancelling ghost orders
ORDER_CONFIRM_TIMEOUT_MS=5000

# Interval in ms between order status polls
ORDER_CONFIRM_POLL_INTERVAL_MS=100

# Solver timeout in seconds
SOLVER_TIMEOUT_SECONDS=30

# Initial capital in USD (used for position sizing and drawdown tracking)
INITIAL_CAPITAL=10000

# Minimum expected profit in USD to consider trading
FW_MIN_PROFIT=0.50

# =============================================================================
# LOGGING
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable JSON logging for production
LOG_JSON=false

# =============================================================================
# EXECUTION MODE
# =============================================================================

# Set to 'paper' for paper trading, 'live' for real trading
TRADING_MODE=paper

# Private RPC endpoint for low-latency submission (optional)
PRIVATE_RPC_URL=
